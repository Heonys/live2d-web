## モバイルチェックリスト

自動画質は Canvas backing buffer を制限しますが、カメラ推論は端末依存です。iOS Safari・Android Chrome 実機測定は0.7の安定化候補であり、0.6ではモバイルを未検証として明記します。

- カメラ・マイク権限には HTTPS を使います。
- ページが hidden の間は capture を止めます。
- 画面回転と background 復帰を確認します。
- 端末で測定してから Worker tracking を検討します。
