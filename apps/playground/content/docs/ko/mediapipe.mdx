## 메인 스레드 추적

> Worker 엔트리를 포함한 트래킹은 모바일·외부 소비자 검증을 마칠 때까지 **experimental**입니다. minor 변경에는 migration을 기록하고 patch에서는 breaking change를 만들지 않습니다.

MediaPipe는 선택 peer이며 해당 subpath에서만 로드됩니다. WASM·Face Landmarker 모델·video frame·스케줄링은 앱이 제공합니다. 트래커는 정규화 값만 보관하고 frame·landmark는 저장하지 않습니다.

```ts
import { createMediaPipeFaceTracker } from
  'live2d-web/tracking/mediapipe'

const tracker = await createMediaPipeFaceTracker({
  modelAssetPath: '/mediapipe/face_landmarker.task',
  wasmPath: '/mediapipe/wasm',
})
const detach = tracker.attach(character, {
  mapping: 'auto',
  channels: { mouth: false },
})

tracker.update(video, performance.now())

detach()
tracker.dispose()
```

## 선택형 Worker

Worker 모드는 추론을 렌더링 스레드에서 분리합니다. update()는 비동기가 되고 한 번에 한 frame만 받아 busy frame은 skipped로 끝냅니다. 시작 실패 시 앱이 main을 명시적으로 선택하며 조용한 fallback은 없습니다. 초기화 제한은 30초, 추론 제한은 10초입니다. timeout은 `tracking-error`로 reject하고 해당 트래커를 종료하므로 앱이 새 Worker 트래커를 만들거나 main 모드로 전환해야 합니다.

```ts
const tracker = await createMediaPipeFaceTracker({
  execution: 'worker',
  modelAssetPath: '/mediapipe/face_landmarker.task',
  wasmPath: '/mediapipe/wasm',
  workerFactory: () => new Worker(
    new URL('./face-tracking.worker.ts', import.meta.url),
    { type: 'module' },
  ),
})
```

## 시작 단계와 모바일 상태

Playground는 카메라 준비, tracker 생성, 첫 inference, 1초 보정과 최종 tracked
전환 시간을 나눠 보여줍니다. 같은 구간의 MediaPipe JavaScript, WASM, task
Resource Timing도 함께 표시하므로 긴 cold start를 곧바로 추론 비용으로
판단하지 말고 어느 단계가 느린지 먼저 확인할 수 있습니다.

warm 로컬 tracker 생성은 3개 데스크톱 엔진에서 중앙값 5초 기대치를 크게
밑돕니다. 이 5초는 CI가 막는 게이트가 아니라 로컬에서 손으로 확인하는
기대치입니다. 트래킹 벤치마크를 실행하는 워크플로는 없습니다. iPhone Safari는 아직 실기기 검증이 필요하고 Android Chrome은
명시적인 미검증 상태입니다. Worker 생성이 실패하거나 timeout이면
`tracking-error`를 표시하고 사용자가 main 모드 tracker를 다시 만들게 하세요.
무한정 기다리거나 조용히 fallback하지 않습니다.
