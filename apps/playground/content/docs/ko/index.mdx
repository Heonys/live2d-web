## 패키지 설치

`live2d-web`만 설치하면 기본 기능을 사용할 수 있습니다. React와 MediaPipe는 선택 의존성이므로, 사용하지 않는 앱의 번들에는 포함되지 않습니다.

```bash
pnpm add live2d-web
```

## 앱이 준비할 것

npm 패키지에는 Cubism Core와 모델 파일이 들어 있지 않습니다. Live2D 약관에 따라 Cubism Core를 준비하고, 사용 권한이 있는 Cubism 4/5 모델과 함께 자신의 서버에서 제공하세요.
